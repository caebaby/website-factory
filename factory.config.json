{
  "_readme": [
    "Role \u2192 CLI routing for factory.js. Any CLI works. Placeholders: {PROMPT} is replaced with the assembled stage prompt.",
    "io: 'stdout' = inputs are embedded in the prompt, stdout becomes the output file (works with ANY model/CLI, no tools needed).",
    "io: 'tools'  = the agent is given file paths and writes the output itself (needs a tool-capable CLI: claude / hermes with hermes-cli toolset / codex).",
    "parse: 'claude-json' only for `claude -p --output-format json` (unlocks cost tracking). Everything else: 'raw'.",
    "Hermes headless is VERIFIED (2026-07-23): `hermes chat -Q -q {PROMPT} -m <model>` \u2014 stdout carries ONLY the final response (session id goes to stderr), so io:'stdout' roles work with any model Hermes can reach (OpenRouter/GLM/Kimi). See 'alternates' below for tested command shapes.",
    "Promotion rule: no model enters a live role until it passes the model-bench packet (benchmarks/model-bench/) or a copy-gate/gauntlet-audited trial run. Alternates are staged, not hired. Audition any candidate: node factory.js audition <alternate-name>.",
    "fallbacks: [names] on a role = runtime auto-selection. Candidates are tried in order when the one before it FAILS to run (spawn error, timeout, empty output). This is availability routing, not quality routing \u2014 quality routing is the leaderboard deciding who holds the primary slot.",
    "The model-bench (benchmarks/model-bench/) is the hiring exam: before promoting a model into a role, run the packet, log the verdict in BUILD_REGISTRY, let the data decide.",
    "Law 10 applies: quality at the LOWEST token cost that clears the gates. Director + critic are the only slots worth premium models."
  ],
  "roles": {
    "research": {
      "label": "research \u2014 Claude Sonnet (tools mode: writes file directly)",
      "cmd": "claude",
      "args": [
        "-p",
        "{PROMPT}",
        "--output-format",
        "json",
        "--model",
        "claude-sonnet-5",
        "--allowedTools",
        "Read,Write,WebSearch,WebFetch",
        "--permission-mode",
        "acceptEdits"
      ],
      "io": "tools",
      "parse": "claude-json",
      "timeoutMin": 30,
      "fallbacks": [
        "hermes-default"
      ]
    },
    "strategy": {
      "label": "strategy \u2014 GLM 4.5-flash via Hermes (free tier, gauntlet-proven vendor)",
      "cmd": "hermes",
      "args": [
        "chat",
        "-Q",
        "-q",
        "{PROMPT}"
      ],
      "io": "stdout",
      "parse": "raw",
      "timeoutMin": 25,
      "fallbacks": [
        "claude-sonnet-cheap"
      ]
    },
    "director": {
      "label": "director \u2014 premium taste tier, runs once per client",
      "cmd": "claude",
      "args": [
        "-p",
        "{PROMPT}",
        "--output-format",
        "json",
        "--model",
        "claude-opus-4-8"
      ],
      "io": "stdout",
      "parse": "claude-json",
      "timeoutMin": 30
    },
    "copy": {
      "label": "copy - DeepSeek V4 Pro via Hermes (budget frontier)",
      "cmd": "hermes",
      "args": [
        "chat",
        "-Q",
        "-q",
        "{PROMPT}",
        "-m",
        "deepseek/deepseek-v4-pro"
      ],
      "io": "stdout",
      "parse": "raw",
      "timeoutMin": 30,
      "fallbacks": [
        "hermes-default"
      ]
    },
    "copygate": {
      "label": "copygate \u2014 Claude Sonnet: DIFFERENT vendor than copy (independence rule)",
      "cmd": "claude",
      "args": [
        "-p",
        "{PROMPT}",
        "--output-format",
        "json",
        "--model",
        "claude-sonnet-5"
      ],
      "io": "stdout",
      "parse": "claude-json",
      "timeoutMin": 15
    },
    "build": {
      "label": "build \u2014 Claude Sonnet tools mode (GLM audition passed via stdout; hermes-tools still UNTESTED)",
      "cmd": "claude",
      "args": [
        "-p",
        "{PROMPT}",
        "--output-format",
        "json",
        "--model",
        "claude-sonnet-5",
        "--allowedTools",
        "Read,Write,Edit",
        "--permission-mode",
        "acceptEdits"
      ],
      "io": "tools",
      "parse": "claude-json",
      "timeoutMin": 40
    }
  },
  "alternates": {
    "_comment": "Tested command shapes, staged for promotion after benching. Copy one over a role's cmd/args/io/parse to swap models.",
    "hermes-glm": {
      "label": "GLM via Hermes \u2014 verified headless 2026-07-23; Anchor v5 was GLM-built",
      "cmd": "hermes",
      "args": [
        "chat",
        "-Q",
        "-q",
        "{PROMPT}",
        "-m",
        "glm-5-turbo"
      ],
      "io": "stdout",
      "parse": "raw",
      "timeoutMin": 30
    },
    "hermes-default": {
      "label": "Hermes default (glm-4.5-flash) \u2014 verified headless + gauntlet-proven 2026-07-23",
      "cmd": "hermes",
      "args": [
        "chat",
        "-Q",
        "-q",
        "{PROMPT}"
      ],
      "io": "stdout",
      "parse": "raw",
      "timeoutMin": 30
    },
    "hermes-tools": {
      "label": "Hermes with file tools for io:'tools' roles \u2014 UNTESTED, verify --yolo/--accept-hooks behavior before live use",
      "cmd": "hermes",
      "args": [
        "chat",
        "-Q",
        "-q",
        "{PROMPT}",
        "-m",
        "glm-5-turbo",
        "-t",
        "hermes-cli",
        "--accept-hooks"
      ],
      "io": "tools",
      "parse": "raw",
      "timeoutMin": 40
    },
    "claude-sonnet-cheap": {
      "label": "Sonnet fallback for any stdout role",
      "cmd": "claude",
      "args": [
        "-p",
        "{PROMPT}",
        "--output-format",
        "json",
        "--model",
        "claude-sonnet-5"
      ],
      "io": "stdout",
      "parse": "claude-json",
      "timeoutMin": 25
    }
  },
  "projects": {
    "_example": {
      "accent": "#C9A227"
    },
    "verdant": {
      "accent": "#C9A24B"
    }
  },
  "_active_preset": "proven-low-cost (Mahdin integration test, 2026-07-27)",
  "researchChain": {
    "enabled": true,
    "cmd": "python3",
    "runner": "/Users/chrisevans/Documents/Claude Projects/playbooks/icp-research-pack/runner/run.py",
    "timeoutMin": 45,
    "_note": "Stage 0b: auto-runs Perplexity+GLM+Gemini deep research when no BLACK_BOOK.md exists. Keys live in the runner .env."
  }
}